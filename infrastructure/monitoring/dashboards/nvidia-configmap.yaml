apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-nvidia
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  nvidia-gpu.json: |
$(cat infrastructure/monitoring/dashboards/nvidia-gpu.json | sed 's/^/    /')
