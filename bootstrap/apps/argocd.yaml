apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://argoproj.github.io/argo-helm
    chart: argo-cd
    targetRevision: 9.x.x
    helm:
      values: |
        server:
          # Deixamos o Helm gerenciar os args básicos para evitar conflito no bootstrap
          config:
            users.anonymous.enabled: "true"
          rbacConfig:
            policy.default: "role:admin"
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated: { prune: true, selfHeal: true }
    # Ignoramos diferenças nos args do server para o ArgoCD não tentar "corrigir" o que o Helm fez
    syncOptions:
      - RespectIgnoreDifferences=true
  ignoreDifferences:
    - group: apps
      kind: Deployment
      name: argocd-server
      jsonPointers:
        - /spec/template/spec/containers/0/args
